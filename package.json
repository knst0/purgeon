{
  "name": "@purgeon/workspace",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "changeset": "changeset",
    "check": "pnpm -r exec tsc --noEmit",
    "fmt": "oxfmt",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "release": "turbo run build && changeset publish",
    "version": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "tsdown": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  },
  "packageManager": "pnpm@11.10.0"
}
